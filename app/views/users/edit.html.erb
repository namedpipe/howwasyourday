<h1>Editing user</h1>

<%= form_for(@user, :url => { :action => "update" }) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
	<div class="send_daily_reminder_field">
	  <%= f.label :send_daily_reminder %><br />
	  <%= f.check_box :send_daily_reminder %>
	</div>
  <div class="actions">
    <%= f.submit :value => "Update" %>
  </div>
<% end %>

<%= link_to 'Back', users_path %>
